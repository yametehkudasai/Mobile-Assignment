import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TabsammplepagePage } from './tabsammplepage.page';

describe('TabsammplepagePage', () => {
  let component: TabsammplepagePage;
  let fixture: ComponentFixture<TabsammplepagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TabsammplepagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
